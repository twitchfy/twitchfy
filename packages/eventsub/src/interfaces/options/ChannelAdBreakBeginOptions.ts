/**
 * The options for subscribing to the ChannelAdBreakBegin event.
 */
export interface ChannelAdBreakBeginOptions {
    /**
     * The broadcaster user ID to listen to ad breaks for.
     */
    broadcaster_user_id: string
}