/**
 * Options for subscribing to the StreamOnline event.
 */
export interface StreamOnlineOptions {
    /**
     * The broadcaster user ID to listen to stream online events for.
     */
    broadcaster_user_id: string
}