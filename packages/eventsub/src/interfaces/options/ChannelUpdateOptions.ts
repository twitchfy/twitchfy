/**
 * The options for subscribing to the ChannelUpdate event.
 */
export interface ChannelUpdateOptions {
    /**
     * The broadcaster user ID to listen to channel update events for.
     */
    broadcaster_user_id: string
}