/**
 * Options for subscribing to the StreamOffline event.
 */
export interface StreamOfflineOptions {
    /**
     * The broadcaster user ID to listen to stream offline events for.
     */
    broadcaster_user_id: string
}